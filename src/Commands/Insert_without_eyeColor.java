/*package Commands;

import work.*;
import personParameters.*;

public class Insert_without_eyeColor extends AbstractCommand {
    *//**
     * @param collection collection
     *//*
    public Insert_without_eyeColor(Manager collection) {
        super(collection);
    }
    public Respond execute(Object argument) {
        Person p = (Person) argument;
        Manager.add(p);
        return new Respond("Элементы был добавлен в коллекцию");
    }
}*/
